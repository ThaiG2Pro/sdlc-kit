---
name: handoff-template
description: >
  Template (skeleton) for _handoff.md — the CPP baton each role agent OVERWRITES at the end
  of its phase to carry reasoning forward. Used by analyst (S2), architect (S3), developer
  (S4/S4-fix), qa (S5). Copy the skeleton, fill every {placeholder}, keep all five section
  headings. The header line "Generated by: {role}" and the five sections are checked
  deterministically by cpp-guard — omit any and the next gate is BLOCKED.
---

<!--
  HOW TO USE
  - Overwrite (do not append) {CHANGE_DIR}/_handoff.md at phase end.
  - The header's role MUST be the agent finishing the phase (analyst|architect|developer|qa)
    — cpp-guard greps the header line for it (e.g. the S3 gate requires the architect's name).
  - Keep ALL five "##" headings verbatim — cpp-guard's S2 gate matches them by name.
  - Retarget the title to the actual transition: S2->S3, S3->S4, S4->S5, S4-fix->S5-retest,
    S5->S6 (GO) or S5->S4-fix (NO-GO).
-->

# Handoff: {FROM-PHASE} → {TO-PHASE}
Generated by: {analyst|architect|developer|qa} | Date: {ISO date}

## 1. Key Decisions (with reasoning)
- {decision}: {what was decided}
  WHY: {reasoning}
  REJECTED: {alternatives considered and discarded}

## 2. Contentious Points
- {AC-ID / ADR / topic}: {what was debated, or where code diverged from design}
  FINAL: {resolution}
  WATCH: {what the next agent must pay attention to}

## 3. Implicit Assumptions
- {assumption not written in any artifact}: {source — user verbal, inferred from code, …}

## 4. Risky Areas
- {area where confidence is lowest}: {why risky, what the next agent should verify}

## 5. Recommended Reading Order
1. {file} §{section} — {why read first}
2. {file} §{section} — {context}
3. Skip: {file/section} — {why not needed}
